# ============================================
# Hajimi King Docker 环境配置示例
# ============================================
# 这是专门为 Docker Compose 环境准备的配置
# 复制此文件为 .env 并根据你的需求修改

# ============================================
# 必需配置
# ============================================
# GitHub API 令牌（至少需要一个）
GITHUB_TOKENS=ghp_your_token_here_1,ghp_your_token_here_2

# ============================================
# 代理配置（使用 warp-proxy）
# ============================================
# 如果使用 docker-compose 中的 warp-proxy 服务
# 使用容器名称进行内部通信（推荐）
PROXY=http://warp-proxy:1080

# 如果需要配置多个代理进行轮换
# PROXY=http://warp-proxy:1080,http://proxy2:8080,http://proxy3:3128

# 如果从宿主机运行（非 Docker）访问 Docker 中的代理
# Windows/Mac: PROXY=http://host.docker.internal:1080
# Linux: PROXY=http://172.17.0.1:1080
# 本地开发: PROXY=http://127.0.0.1:1080

# ============================================
# 搜索配置
# ============================================
QUERIES_FILE=queries.txt
DATE_RANGE_DAYS=730
FILE_PATH_BLACKLIST=readme,docs,doc/,.md,example,sample,tutorial,test,spec,demo,mock
HAJIMI_CHECK_MODEL=gemini-2.0-flash-exp

# ============================================
# 性能配置
# ============================================
HAJIMI_MAX_WORKERS=10
HAJIMI_BATCH_SIZE=10
HAJIMI_BATCH_INTERVAL=60

# ============================================
# 数据存储
# ============================================
DATA_PATH=./data

# ============================================
# 外部同步（可选）
# ============================================
GEMINI_BALANCER_SYNC_ENABLED=false
GEMINI_BALANCER_URL=
GEMINI_BALANCER_AUTH=

GPT_LOAD_SYNC_ENABLED=false
GPT_LOAD_URL=
GPT_LOAD_AUTH=
GPT_LOAD_GROUP_NAME=

# ============================================
# Docker 特定配置
# ============================================
TZ=Asia/Shanghai
LOG_LEVEL=INFO

# ============================================
# 启动说明
# ============================================
# 1. 启动包含代理的完整服务：
#    docker-compose --profile proxy up -d
#
# 2. 只启动主服务（不含代理）：
#    docker-compose up -d
#
# 3. 验证代理是否工作：
#    docker exec hajimi-king curl -x http://warp-proxy:1080 https://api.github.com
#
# 4. 查看日志：
#    docker-compose logs -f hajimi-king
#    docker-compose logs -f warp-proxy
# ============================================